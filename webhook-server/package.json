{
  "name": "axiom-webhook-server",
  "version": "1.0.0",
  "description": "AXIOM TradingView webhook receiver — real-time signal SSE server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "groq-sdk": "^0.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}

